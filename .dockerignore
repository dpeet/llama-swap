# Keep the build context small/reproducible for docker/Dockerfile.gb10-serve.
.git
build/
ui/node_modules/
# ui_dist is regenerated by the ui-builder stage; never ship a stale local copy.
internal/server/ui_dist/
